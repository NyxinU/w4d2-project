<h1>Here are your cats</h1>
<br>
<% @cats.each do |cat| %>
  <%= cat.name %>
  <%= link_to "Show Cat", cat_url(cat) %>
  <%= button_to "Delete Cat", cat_url(cat), :method => :delete %>
  <br>
<% end %>
<%= button_to "Rent a cat", new_cat_rental_request_url, action: "new", :method => :get%>
